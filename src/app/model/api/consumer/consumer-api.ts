import { GameStateManeger } from "../../models/game/game-state";
import { Observable } from "@reactivex/rxjs";

export class ConsumerApi {
    constructor(private _state:GameStateManeger) {
        
    }
    
    // get player():Observable<string>{
        
    // }
}